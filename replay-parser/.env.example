# Porta HTTP (injetada automaticamente pelo Render — não precisa setar)
PORT=5000

# Origins permitidas no CORS — separar por vírgula se necessário
# Padrão: * (aceita qualquer origin)
# Em produção: usar a URL do Vercel — ex: https://sua-app.vercel.app
ALLOWED_ORIGIN=*
